#include<stdio.h>
#include<stdlib.h>
struct Tree
{
    int data;
    struct Tree *left;
    struct Tree *right;
};
int main()
{
    int item;
    struct Tree *t = NULL;
    scanf("%d", &item);
    while(item != -1)
    {
        scanf("%d", &item);
    }
    if(root->data == NULL)
    {
        
    }
}
